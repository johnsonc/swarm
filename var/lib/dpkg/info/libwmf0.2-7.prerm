#!/bin/sh
set -e
# Automatically added by dh_installdefoma
case "$1" in
	upgrade)
		if [ -x "`which defoma-app 2>/dev/null`" ]; then
			defoma-app clean libwmf0.2-7
		fi
		;;
	remove)
		if [ -x "`which defoma-app 2>/dev/null`" ]; then
			defoma-app purge libwmf0.2-7
		fi
esac
# End automatically added section
