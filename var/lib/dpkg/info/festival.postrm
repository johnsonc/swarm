#!/bin/sh

set -e

rm -f /etc/init.d/festival

if [ "$1" = "purge" ];then
    rm -rf /var/log/festival
    rm -f /etc/festival.scm
fi

# Automatically added by dh_installcatalogs
if [ "$1" = "purge" ]; then
	rm -f /etc/sgml/festival.cat /etc/sgml/festival.cat.old
fi
# End automatically added section

