#!/bin/sh
set -e
# Automatically added by dh_installinit
if [ -x "/etc/init.d/bluetooth" ]; then
	if [ -x "`which invoke-rc.d 2>/dev/null`" ]; then
		invoke-rc.d bluetooth stop || exit $?
	else
		/etc/init.d/bluetooth stop || exit $?
	fi
fi
# End automatically added section
