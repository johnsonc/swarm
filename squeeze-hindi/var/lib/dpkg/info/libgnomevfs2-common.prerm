#!/bin/sh
set -e
# Automatically added by dh_gconf
if [ "$1" = remove ] || [ "$1" = upgrade ]; then
	gconf-schemas --unregister system_smb.schemas desktop_gnome_url_handlers.schemas system_dns_sd.schemas system_http_proxy.schemas desktop_default_applications.schemas 
fi
# End automatically added section
